# torch/torchvision are deliberately unpinned to a build — install the CPU or
# CUDA wheel that matches your machine BEFORE this file, e.g.
#   pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
torch>=2.2
torchvision>=0.17

numpy>=1.26
opencv-python-headless>=4.9

# geometric/color augmentation only — the weather corruptions are hand-written
# in src/weather.py and stay outside the bbox-transforming machinery
albumentations>=1.4,<2.0

# metrics. faster-coco-eval backs MeanAveragePrecision: a drop-in for
# pycocotools, roughly 10x quicker, which matters when every run evaluates
# nine separate test conditions.
torchmetrics>=1.3
faster-coco-eval>=1.6

mlflow>=2.12,<3.0
matplotlib>=3.8

# serving
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# dev
pytest>=8.1
httpx>=0.27
